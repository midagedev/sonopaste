{
  "name": "sonopaste",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:gh": "VITE_BASE_PATH=/ npm run build",
    "build:cf": "VITE_BASE_PATH=/ npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
