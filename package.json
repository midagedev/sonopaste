{
  "name": "jazzmodem",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:cf": "VITE_BASE_PATH=/ npm run build",
    "build:tests": "tsc -p tsconfig.test.json",
    "test": "npm run build:tests && node --test .test-build/tests/**/*.test.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
